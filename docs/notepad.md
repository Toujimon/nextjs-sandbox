# Notepad

## Development Plan

### Google APIs

I just like the idea of apps that exploit the Google APIs to use documents, spreadsheets and drive as long term data storage.
I don't so much have an application as I want to know how to make use of the auth system and the API's themselves.

### Styling

#### Enhance design
Let's make the page look a little better if possible. I prefer functionality over styling, but it would not hurt.

#### JS to CSS tech
Maybe consider switching to another JS-to-CSS sytem (tailwind, css-vanilla-inject).

#### Try out CSS features
At least consider to try out some CSS features I have never given that much tought, or specific complex scenarios.
- Container related media queries
- ...

### Server app feathres

NextJS works nicely, I should eithed consider to exploit some of its features. Let's find out about NextJS 16 (or later if already implemented).

